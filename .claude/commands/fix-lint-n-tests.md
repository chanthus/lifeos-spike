# Fix lint and tests

Run "pnpm check" and fix all issues identified.
Run "pnpm test" and fix all failing tests.
Make sure to run "pnpm check" and "pnpm test" afterwards to verify that everything is fixed.
Keep going until everything is fixed.

$ARGUMENTS
